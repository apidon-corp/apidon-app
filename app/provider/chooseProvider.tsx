import { View, Text } from "react-native";
import React from "react";

type Props = {};

const chooseProvider = (props: Props) => {
  return (
    <View
      style={{
        width: "100%",
        flex: 1,
      }}
    >
      <Text>chooseProvider</Text>
    </View>
  );
};

export default chooseProvider;
